interface:
  display_name: "Requirement Calibration"
  short_description: "Calibrate scope, ownership, and acceptance before coding"
  default_prompt: "Use $requirement-calibration to calibrate this repository change's requirements and boundaries before implementation."

policy:
  allow_implicit_invocation: false
