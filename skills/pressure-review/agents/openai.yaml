interface:
  display_name: "Pressure Review"
  short_description: "Independently find acceptance blockers and close the loop through rework, re-verification, and dispute handling"
  default_prompt: "Use $pressure-review to pressure-test this repository change independently and resolve substantiated findings."

policy:
  allow_implicit_invocation: false
