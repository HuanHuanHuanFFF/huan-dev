interface:
  display_name: "Prompt Entropy"
  short_description: "Compress tasks into dense agent prompts"
  default_prompt: "Use $prompt-entropy to turn this task into a compact, high-information prompt for another agent."
policy:
  allow_implicit_invocation: true
