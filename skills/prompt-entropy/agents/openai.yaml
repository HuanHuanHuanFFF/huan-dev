interface:
  display_name: "Prompt Entropy"
  short_description: "Write dense, reliable prompts for agents and skills"
  default_prompt: "Use $prompt-entropy to write or refine this prompt while preserving intent, constraints, and verification."
policy:
  allow_implicit_invocation: true
