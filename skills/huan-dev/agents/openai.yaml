interface:
  display_name: "Huan Dev"
  short_description: "从需求边界确认到独立压力审查与证据化交付的轻量开发工作流"
  default_prompt: "使用 $huan-dev 完成这次仓库改动，在必要时澄清边界，并对非简单改动执行独立压力 Review。"

policy:
  allow_implicit_invocation: false
